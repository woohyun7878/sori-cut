name: Pages Domain Reachability Check

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: read

# Prevent overlapping scheduled runs from piling up if a check is slow.
concurrency:
  group: pages-uptime-check
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check public endpoints
        shell: bash
        run: |
          set -euo pipefail

          urls=(
            "https://soricut.studio/"
            "https://www.soricut.studio/"
          )

          for url in "${urls[@]}"; do
            code="$(curl --silent --show-error --location --output /dev/null --write-out "%{http_code}" --max-time 20 "$url")"
            echo "$url -> HTTP $code"
            if [[ "$code" -lt 200 || "$code" -ge 400 ]]; then
              echo "Reachability check failed for $url"
              exit 1
            fi
          done
